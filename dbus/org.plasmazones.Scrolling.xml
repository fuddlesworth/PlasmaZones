<!DOCTYPE node PUBLIC "-//freedesktop//DTD D-BUS Object Introspection 1.0//EN"
    "http://www.freedesktop.org/standards/dbus/1.0/introspect.dtd">
<!--
SPDX-FileCopyrightText: 2026 fuddlesworth
SPDX-License-Identifier: GPL-3.0-or-later
-->
<node>
    <interface name="org.plasmazones.Scrolling">
        <annotation name="org.gtk.GDBus.DocString" value="Scroll-engine-specific surface. Window lifecycle and tile-request traffic for scrolling screens rides org.plasmazones.Tiling (the shared tiling-family pipeline, which routes per screen). This interface carries the scrolling screen set, the strip snapshot the settings app renders as its preview surface (plus the preset vocabulary beside it), the wheel-driven focusColumn verb, the four absolute width/height setters for external scripting, the clearWindowedFullscreen reconciliation call the KWin effect sends when a client leaves fullscreen on its own, and the reapplyWindowGeometry repair call it sends on a fullscreen exit whose strip rects never moved."/>

        <property name="scrollingScreens" type="as" access="read">
            <annotation name="org.gtk.GDBus.DocString" value="List of screen IDs currently using the scrolling engine, sorted, matching the ordering of the scrollingScreensChanged payload so a consumer can compare the two for the same set. The KWin effect uses it as the discriminator that stamps Mode 'scrolling' rather than 'tiling' in window rule queries, but it does not answer from this raw set: it intersects it at read time with the engine-managed union published on org.plasmazones.Tiling, because the two sets arrive as independent signals with no ordering guarantee and this one can transiently name a screen the union has already dropped. Changes are announced on the scrollingScreensChanged signal below, not through org.freedesktop.DBus.Properties.PropertiesChanged, which this property does not emit."/>
        </property>

        <property name="scrollEffectBehaviour" type="a{sv}" access="read">
            <annotation name="org.gtk.GDBus.DocString" value="The two scrolling behaviours the COMPOSITOR owns, as already-resolved screen-id lists: {'focusFollowsMouse': as, 'cropStraddlers': as}. Both are per-context rule slots (SetScrollFocusFollowsMouse / SetScrollCropStraddlers) whose consumer lives in the KWin effect rather than the scroll engine, so unlike their four sibling behaviour slots they cannot ride the engine's per-screen override map. The daemon resolves rule-then-config per scrolling screen and publishes the RESOLVED membership, which keeps the effect free of any config knowledge: it answers each question with a set lookup and holds no fallback of its own. A screen absent from a list has that behaviour off; a screen that is not scrolling appears in neither. Both lists are sorted, matching the scrollEffectBehaviourChanged payload. One map rather than two list properties so the compositor pays a single bring-up query and a single subscription. Changes are announced on scrollEffectBehaviourChanged below, not through org.freedesktop.DBus.Properties.PropertiesChanged, which this property does not emit."/>
        </property>

        <method name="focusColumn">
            <annotation name="org.gtk.GDBus.DocString" value="Focus the adjacent column on a scrolling screen. Called by the KWin effect's Meta+wheel axis shortcut with the cursor's screen. A press at the strip's edge CROSSES to the adjacent output when one exists — including a neighbour running a different mode, where the daemon activates that engine's entry-edge window — and a press on an EMPTY scrolling screen crosses the same way instead of dead-ending, so a wheel notch can move focus to another monitor. Every rejected call is a silent no-op with no error reply: an empty screenId, a screen the scrolling engine does not own, and any delta other than -1 or +1 are all ignored, so a caller cannot tell a refusal from a call that landed on an empty strip."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose strip should move (the cursor's screen). An empty string is ignored."/>
            </arg>
            <arg name="delta" type="i" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="-1 focuses the column to the left, +1 to the right. Any other value is ignored."/>
            </arg>
        </method>

        <method name="setColumnWidthProportion">
            <annotation name="org.gtk.GDBus.DocString" value="Set the focused column's width to a fraction of the work area (niri set-column-width with an absolute value; the shortcut surface carries no value argument, so the absolute setters live here). Same silent ownership gate as focusColumn, and the value is refused silently outside the proportion range the settings UI enforces (0.05 to 1.0). A value equal to the current intent answers with a no-target OSD rather than acting, like the step verbs, and a successful call also raises the navigation OSD — every one of the four setters reports through it when that setting is on, so a scripted caller should expect on-screen feedback per call."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose focused column to resize. An empty string is ignored."/>
            </arg>
            <arg name="proportion" type="d" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Work-area fraction, 0.05 to 1.0."/>
            </arg>
        </method>

        <method name="setColumnWidthPixels">
            <annotation name="org.gtk.GDBus.DocString" value="Set the focused column's width to a fixed pixel value. Same gates, equal-value no-target answer and OSD feedback as setColumnWidthProportion; the value is refused silently outside the fixed range the settings UI enforces (100 to 10000). The relayout still caps a rendered column at the work-area width."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose focused column to resize. An empty string is ignored."/>
            </arg>
            <arg name="px" type="i" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Width in pixels, 100 to 10000."/>
            </arg>
        </method>

        <method name="setWindowHeightProportion">
            <annotation name="org.gtk.GDBus.DocString" value="Set the focused window's height intent to a fraction of the work area (niri set-window-height with an absolute value). Same gates, proportion range, equal-value no-target answer and OSD feedback as setColumnWidthProportion. Heights have no exact-proportion kind in the strip model: the value is stored as a fraction anchor that snaps to the NEAREST entry of the screen's effective height preset list at relayout, the same value-anchored contract the cycle-height shortcut walks. For an exact height use setWindowHeightPixels. The relayout renormalizes heights into the column budget, so an oversized value degrades rather than overflowing."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose focused window to resize. An empty string is ignored."/>
            </arg>
            <arg name="proportion" type="d" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Work-area fraction, 0.05 to 1.0."/>
            </arg>
        </method>

        <method name="setWindowHeightPixels">
            <annotation name="org.gtk.GDBus.DocString" value="Set the focused window's height to a fixed pixel value. Same gates, equal-value no-target answer and OSD feedback as setWindowHeightProportion; the value is refused silently outside the fixed range the settings UI enforces (100 to 10000)."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose focused window to resize. An empty string is ignored."/>
            </arg>
            <arg name="px" type="i" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Height in pixels, 100 to 10000."/>
            </arg>
        </method>

        <method name="clearWindowedFullscreen">
            <annotation name="org.gtk.GDBus.DocString" value="Drops a window's windowed-fullscreen flag. Called by the KWin effect when a windowed-fullscreen client leaves fullscreen on its own, so the strip's flag follows reality. Silent no-op for an unknown window or one whose flag is not set."/>
            <arg name="windowId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Window whose flag to clear. An empty string is ignored."/>
            </arg>
        </method>

        <method name="reapplyWindowGeometry">
            <annotation name="org.gtk.GDBus.DocString" value="Re-emits a window's true strip rect. Called by the KWin effect when the compositor moved the window behind the engine's back (KWin restores a fullscreen-exiting window to its pre-fullscreen rect one client round-trip after the strip already placed it, and the engine's emit-on-change gate would otherwise stay silent forever). The engine evicts the window's emit-gate memory and relayouts its screen so the next batch re-carries the rect. Silent no-op for an unknown window."/>
            <arg name="windowId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Window to re-emit. An empty string is ignored."/>
            </arg>
        </method>

        <method name="visibleStripJson">
            <annotation name="org.gtk.GDBus.DocString" value="The strip as it currently looks on a screen: a JSON array of visible tile rects plus zoneNumber, the tile's 1-based visible slot in strip order. Each rect is {x, y, width, height}, 0.0 to 1.0 per axis, normalized against the screen's FULL geometry. The tiles themselves are clipped to the work area after the outer gaps are subtracted, so the fractions show the panel gap and a consumer maps back to pixels by scaling against the screen rectangle. Same basis as the daemon's own OSD strip card. Only what is on screen is listed. Hidden tabs of a tabbed column, minimized tiles, parked columns, and tiles whose intersection with the work area is empty (a stack whose minimum heights overflow the work area resolves its tail below the bottom edge) are all left out and carry no number. A partially visible column is clipped to the work area rather than dropped, with no minimum-visibility threshold: an arbitrarily thin sliver still gets its own number. zoneNumber is the scroll zone number, and the Snap-to-Zone shortcuts cover 1 to 9, so a strip showing more than nine tiles numbers them all but only the first nine have a digit that reaches them. The settings app renders the array where the other modes show a layout thumbnail. Empty array when the screen has no strip or is not scrolling."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose strip to describe. An empty string yields an empty array."/>
            </arg>
            <arg name="stripJson" type="s" direction="out">
                <annotation name="org.gtk.GDBus.DocString" value="JSON array string."/>
            </arg>
        </method>

        <method name="presetVocabularyJson">
            <annotation name="org.gtk.GDBus.DocString" value="The screen's effective preset vocabulary as a JSON object {&quot;columnWidths&quot;: [...], &quot;windowHeights&quot;: [...]} of 0.0 to 1.0 fractions. Each list resolves independently: a list the context's resolved scrolling template supplies comes from that template's own preset list, and a list it does not supply falls back to the configured preset list, so a template that defines widths but no heights yields template widths beside the configured heights. This is the vocabulary the cycle-preset-width and cycle-preset-height shortcuts walk on that screen. Same silent ownership gate as focusColumn: an empty object when the screen is not scrolling."/>
            <arg name="screenId" type="s" direction="in">
                <annotation name="org.gtk.GDBus.DocString" value="Screen whose vocabulary to describe. An empty string yields an empty object."/>
            </arg>
            <arg name="vocabularyJson" type="s" direction="out">
                <annotation name="org.gtk.GDBus.DocString" value="JSON object string."/>
            </arg>
        </method>

        <method name="scrollTabSurfaces">
            <annotation name="org.gtk.GDBus.DocString" value="Every live tab-indicator surface, as a map of effective screen id to wl_surface protocol object id. The replay half of scrollTabSurfaceChanged, for a compositor-side consumer that starts (or restarts) after the surfaces already exist and would otherwise wait for an announcement that only fires on change."/>
            <arg name="surfaces" type="a{sv}" direction="out">
                <annotation name="org.gtk.GDBus.DocString" value="Map of effective screen id to wl_surface object id."/>
            </arg>
        </method>

        <signal name="scrollTabSurfaceChanged">
            <annotation name="org.gtk.GDBus.DocString" value="The wl_surface drawing a screen's scrolling tab indicators changed. The compositor slides that surface with the scrolling strip so the indicators travel with the columns they label, and the object id is the only handle it can match on: every one of the daemon's overlays reports the same window class, and a layer surface's scope is not exposed per window. A surfaceId of 0 retracts the registration, and one always precedes the surface's destruction, because Wayland reuses object ids and a stale registration would come to name an unrelated surface."/>
            <arg name="screenId" type="s">
                <annotation name="org.gtk.GDBus.DocString" value="Effective screen id."/>
            </arg>
            <arg name="surfaceId" type="u">
                <annotation name="org.gtk.GDBus.DocString" value="wl_surface protocol object id, or 0 when the surface is gone."/>
            </arg>
        </signal>

        <signal name="scrollingScreensChanged">
            <annotation name="org.gtk.GDBus.DocString" value="Emitted when the set of screens using the scrolling engine changes."/>
            <arg name="screenIds" type="as">
                <annotation name="org.gtk.GDBus.DocString" value="List of screen IDs currently using the scrolling engine."/>
            </arg>
        </signal>

        <signal name="scrollEffectBehaviourChanged">
            <annotation name="org.gtk.GDBus.DocString" value="Emitted when the resolved per-screen behaviour the compositor owns changes. The payload is the WHOLE map, never a delta, so a receiver that missed an earlier emission never has to merge against a stale copy."/>
            <arg name="behaviour" type="a{sv}">
                <annotation name="org.gtk.GDBus.DocString" value="Same shape as the scrollEffectBehaviour property."/>
            </arg>
        </signal>
    </interface>
</node>
