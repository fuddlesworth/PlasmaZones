// SPDX-FileCopyrightText: 2026 fuddlesworth
// SPDX-License-Identifier: GPL-3.0-or-later

/**
 * @brief Snapping → Layouts: the snapping zone-layout library.
 *
 * Thin host fixing the shared browser to the snapping view; everything else
 * (search, filters, grouping, import/create/delete) lives in
 * LayoutBrowserPage.
 */

LayoutBrowserPage {
    viewMode: 0
}
